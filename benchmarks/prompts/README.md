# Benchmark prompts

Fixed prompt token IDs will be added only after the pinned tokenizer is verified. Raw-text-only fixtures would not
isolate tokenizer changes and are therefore not used as benchmark inputs.

